## 1. 基线与失败复现

- [x] 1.1 记录 `visualization_app/`、交付目录外置文件、主程序 EXE 和 local helper EXE 的初始 SHA-256 与 Git 状态，并验证报告能够区分用户既有改动和本变更新改动
- [x] 1.2 为 50 Hz 持续输入、ACK 延迟和断线重连补充 helper 传输失败测试，并验证旧实现可稳定复现固定一秒发送或增长性积压而测试不会依赖真实硬件
- [x] 1.3 为非 `local_admin` Bootstrap 补充禁止 PLC/ABB/RTSP 发现的计时与调用测试，并验证旧实现会调用至少一个本机发现入口
- [x] 1.4 为授权远程角色选择客户端 CSV/文件夹补充前后端失败测试，并验证旧实现会请求服务器选择器或把客户端路径作为服务器路径
- [x] 1.5 为 MySQL 作用域、无配置新电脑、DPAPI 解密失败、1045 和服务未启动补充失败测试，并验证每种错误能被独立断言
- [x] 1.6 为本地诊断先行、默认单次模型调用、90 秒服务端时限、120 秒浏览器时限和证据指纹缓存补充失败测试，并使用伪模型验证测试不访问真实外部 API

## 2. 远程采集传输

- [x] 2.1 在 helper 实现单在途、ACK 驱动、自适应有界批次的数据泵，并验证 50 Hz、10 分钟虚拟时钟测试无丢失、无重复、无增长性积压且 P95 延迟不超过 1 秒
- [x] 2.2 保留断线重发与服务端幂等确认，增加队列深度、未确认批次、确认序号和各阶段时间戳，并验证重连测试只写入一次且状态接口不含样本内容或凭据
- [x] 2.3 为远程会话增加单调采集/状态版本和按版本缓存的实时载荷，使用轻量心跳处理空闲连接，并验证无新数据时聚合/预测调用次数不增长、新数据到达时只生成一次新版本载荷
- [x] 2.4 让 Bootstrap 仅对回环 `local_admin` 执行服务器本机设备发现，并验证远程角色响应不触发探测且本机管理员现有发现测试继续通过
- [x] 2.5 运行 `test_helper_transport.py`、`test_helper_websocket.py`、`test_local_capture_agent.py`、`test_websocket_live.py` 和 `test_interface_agent.py`，并验证全部通过且没有修改传感器解析或预测函数

## 3. 远程模拟数据源

- [x] 3.1 实现会话隔离的浏览器数据源暂存与不透明标识，加入扩展名、数量、单文件/总量、相对路径和路径穿越校验，并验证跨会话标识、非法类型和越界请求全部被拒绝
- [x] 3.2 增加授权用户和 LAN 操作员的浏览器文件/文件夹上传接口，将不透明标识解析到现有模拟采集器，并验证 API 响应不泄露服务器暂存绝对路径
- [x] 3.3 调整前端角色分流，使除 `local_admin` 外的角色均使用浏览器选择器，并对手填客户端绝对路径返回专用中文提示；验证选择或上传不会隐式启动采集
- [x] 3.4 实现会话退出、超时和数据源替换清理，并验证只删除目标会话暂存资源且不影响其他会话或用户提供的原文件
- [x] 3.5 运行 `test_guest_web.py`、`test_frontend_guest_simulation.py`、`test_public_web_security.py`、`test_app.py` 和新增模拟源测试，并验证本机管理员原生选择器路径仍通过

## 4. MySQL 作用域与换机初始化

- [x] 4.1 在配置和状态模型中加入 `helper_local` 与 `server_target` 作用域及安全元数据，并验证界面和 API 始终显示实际执行机器、主机、端口和数据库而不返回密码
- [x] 4.2 复用 helper 的 Windows 当前用户 DPAPI 机制实现本机 MySQL 凭据保存、读取状态和不可解密降级，并验证配置文件中无明文密码、复制密文到不同保护上下文后显示未配置
- [x] 4.3 增加 helper 本机 MySQL 预检和有界写入命令，保持 CSV 与既有待补传降级链路，并验证数据库失败不会终止采集、预测或 CSV 保存
- [x] 4.4 在前端增加本机 MySQL 首次配置、保存和预检流程，服务器仅转发瞬时凭据且不得落盘或记日志，并通过日志捕获测试验证提交密码不可检索
- [x] 4.5 扩充 MySQL 错误分类和中文操作建议，验证 1045 同时覆盖密码与 `用户@来源主机` 授权、连接拒绝指向服务/端口、未知数据库和驱动缺失各自独立
- [x] 4.6 运行 `test_remote_mysql_setup.py`、`test_mysql_visibility.py`、`test_mysql_identity.py`、`test_mysql_diagnostics.py`、`test_mysql_credentials.py` 和新增 helper MySQL 测试，并验证服务器目标 MySQL 现有行为不变

## 5. 响应式 Agent 诊断

- [x] 5.1 将诊断提交拆为冻结本地证据快照、即时本地结果和异步模型任务，并验证外部模型阻塞时 API 和界面仍能立即显示本地事实及任务状态
- [x] 5.2 实现默认确定性工具收集加一次模型综合的快速路径，并将现有模型规划加综合保留为显式深度模式；验证默认路径模型调用次数不超过一次、深度模式阶段可见
- [x] 5.3 实现连接/读取/阶段超时、90 秒服务器总时限与无同阶段无界重试的降级状态，并验证伪模型超时后任务终止且本地结果保持可用
- [x] 5.4 实现按会话、证据指纹、模型和安全配置摘要的短期成功结果缓存，并验证相同证据命中、数据或告警变化失效、缓存键不含 API 密钥
- [x] 5.5 调整浏览器轮询在 120 秒内结束，显示阶段、耗时、缓存命中和模型辅助证据边界，并验证迟到结果不会覆盖较新任务或被描述为已确认缺陷
- [x] 5.6 运行 `test_agentic_diagnosis.py`、诊断作业测试、前端诊断测试和本地规则相关回归，并验证本地模式不需要 API 密钥且模型失败不会阻塞页面

## 6. 安全与核心回归

- [x] 6.1 运行公网身份、访问角色、WebSocket 会话隔离、路径穿越和日志脱敏安全测试，并验证远程新增接口不能绕过配对或跨会话读取资源
- [x] 6.2 运行采集完整性、17 通道模型输入、M3232、健康指标、预警、CSV 结构和 MySQL 表结构相关现有测试，并验证本变更前后黄金结果一致
- [x] 6.3 运行 `visualization_app` 全量自动测试并保存机器可读结果；若存在与本变更无关的既有失败，必须记录用例、基线复现和不受影响证据，不能静默忽略
- [x] 6.4 按 `release-regression-harness` 规格执行现有核心基线和启动检查；若该 harness 尚未实现，则执行其覆盖的组成命令并在报告中标记替代路径

## 7. 构建与交付同步

- [x] 7.1 通过外置文件明确清单将验证后的 Python/HTML/JavaScript 同步到 `delivery/AFP_Integrated_System_Modular_v2.0.3_Agentic/app/`，并验证源码与交付副本逐文件 SHA-256 一致
- [x] 7.2 使用 `visualization_app/build_local_capture_helper.ps1` 的既有 Python 3.11/PyInstaller 工具链重建 local helper，验证干净目录启动、版本/能力握手和无新增第三方运行时依赖
- [x] 7.3 替换交付目录的 `local_helper/AFP_Local_Capture_Helper.exe`，记录旧新 SHA-256、构建命令与原因，并验证 `AFP_Integrated_System_Modular.exe` 的 SHA-256 与基线完全一致
- [x] 7.4 更新交付哈希清单、版本说明和本机 MySQL 初次配置说明，并验证清单覆盖所有变更文件且不包含密码、令牌或访问密钥

## 8. 本机与异机验收

- [x] 8.1 在开发机从交付目录启动主程序和新 helper，完成模拟采集、真实接口无硬件降级、CSV 保存、数据库失败降级和本地诊断检查，并保存日志、端口和版本证据
- [ ] 8.2 通过 `https://desktop-410sfvi.tail97fe2c.ts.net/` 在开发机执行公网链路测试，验证远程 Bootstrap 不再等待设备探测、50 Hz 受控输入无增长性积压、浏览器文件可用且诊断先显示本地结果
- [x] 8.3 在开发机用隔离 Windows 用户或清空的 helper 配置目录模拟新电脑首次运行，验证必须重新配置本机 MySQL、旧机 DPAPI 密文不会回退为空密码、配对与会话隔离正常
- [x] 8.4 生成第二台电脑验收包和逐步清单，包含安装/启动 helper、配对、10 分钟 50 Hz 测试、CSV/文件夹上传、本机 MySQL 预检、快速诊断、日志导出和预期结果，并验证清单不要求访问服务器文件系统
- [x] 8.5 汇总自动化、本机集成、本机公网和异机现场四级证据；在用户实际返回第二台电脑结果前，将异机结论明确标记为“待现场确认”，返回后再记录通过或失败及对应日志

## 9. 用户反馈回归修复（2026-09-22）

- [x] 9.1 添加五类接口、远程模拟停止租约、MySQL 配置保存及表单轮询覆盖的失败测试
- [x] 9.2 修复模拟停止并保存的状态反馈和会话控制，保持真实硬件租约保护
- [x] 9.3 恢复五类接口的独立显示、协议和后端模拟元数据，验证切换模式不改变真实驱动
- [x] 9.4 修复 MySQL 预检与实际保存不一致以及本机表单被定时覆盖问题，验证失败降级不影响 CSV
- [x] 9.5 用本变更 SPEC 重跑针对性及重要功能回归，并比较源码、交付文件与 EXE 哈希
- [x] 9.6 同步交付目录、更新清单并在本机执行模拟停止/保存及 MySQL 测试；异机硬件和数据库现场结果单独标注待用户验收
- [x] 9.7 复现默认预测页 WebSocket 非有限数值编码错误，在实时传输出口复用 HTTP 的 JSON 安全转换，验证 NaN/Inf 变为 null 而预测算法不变
- [x] 9.8 主程序再次重启后，在公网浏览器复验默认预测预警页面不再出现 JSON 编码错误，并核对未授权本机目录时的保存提示
- [x] 9.9 复现授权公网 Bootstrap 丢失五类静态传感器目录的回归，只恢复静态类型与默认接口而保持服务器硬件探测关闭，并验证物理接口清单仍为空
- [ ] 9.10 主程序加载更新后，在授权公网网页核对五类传感器类型与软件端一致，并复测模拟停止保存、文件选择、MySQL 状态及诊断降级
- [x] 9.11 复现访客浏览器本地回放期间采集状态冻结在 0 点，增加至多每秒一次的会话状态刷新，并验证停止后的迟到响应不覆盖最终结果

## 10. 目标 MySQL 预检与跨电脑真实保存一致（待实现）

- [x] 10.1 先补失败测试：服务器已存有效目标密码而网页密码框为空时预检返回 1045；自定义目标不得借用其它配置密码；显式错误密码不得静默回退；真实 helper 路径的目标库写入端与 SPEC 不一致
- [x] 10.2 实现授权会话的非秘密目标配置标识和服务器侧凭据解析，使目标库预检、模拟启动及最终写入引用同一配置；不向浏览器、helper、状态、日志或样本文件返回目标密码，并保持访客禁用
- [x] 10.3 为真实 helper 批次增加会话与 `capture_uuid` 隔离、磁盘配额受控的服务器完整样本记录；仅在持久接受后 ACK，按序号幂等处理重复和断线重发，实时镜像仍仅用于显示与预测
- [x] 10.4 真实采集停止时等待最终状态与全部批次确认，再由服务器用现有表结构和 `MySQLCaptureStore.save_layer` 写目标库；helper 只写本机 CSV/本机 MySQL；数据库失败保留可重试、无密码的有界待补传记录，且 CSV 与预测不受影响
- [x] 10.5 调整网页目标库预检与结果展示，明确服务器执行端、空密码框的已存配置语义、目标库和本机库各自的预检/最终保存状态；远程模拟写 helper-local MySQL 的既有显式限制保持不变
- [x] 10.6 运行红绿回归：真实与模拟目标库预检/停止写入一致、超过 50,000 点完整保存且终结时内存受限、重复批次与重复停止无重复/缺失、磁盘配额和数据库故障降级、跨会话隔离及无密码泄漏；再跑五类接口、CSV、预测和现有 MySQL 相关测试
- [x] 10.7 只同步验证过的源码到既定交付目录，按实际打包边界决定是否重建 helper，更新哈希清单及异机验收清单；保持主程序 EXE 与五类传感器协议/解析不变，并运行交付完整性与启动检查
- [ ] 10.8 本机经公网分别验证授权模拟与真实 helper 的目标库预检、停止写入和故障状态；在另一台电脑验证目标库无需重新分发密码且数据行数完整。本机模拟、伪库与代码测试不得替代真实异机数据库验收

## 11. 公网复测：模拟启动门禁、延迟与 helper 掉线（2026-09-23）

- [x] 11.1 增加失败测试并复现：授权模拟被 `hardwareCheckInProgress` 阻止、旧 WebSocket 清理误伤新连接、服务端并发发送同一 helper 连接可能交错
- [x] 11.2 让授权模拟、LAN 模拟和本机模拟只做模拟源通道匹配，不获取真实控制租约、不等待或触发物理接口检查；保持真实采集门禁不变
- [x] 11.3 让 helper 断线重连按发送器身份清理连接，并串行化服务端同一 WebSocket 的全部发送；验证旧连接退出后新连接仍在线且可接收命令
- [x] 11.4 重跑 helper 传输、50 Hz 延迟、模拟采集、五类接口及公网安全回归，确认无增长性积压且不修改传感器协议、解析和预测
- [x] 11.5 同步交付目录、更新缓存标识和哈希清单，重启主程序并在无需真实硬件的范围内验证公网新资源、模拟启动与 helper 稳定连接；授权会话和异机真实采集仍按现场证据单列
- [x] 11.6 增加授权远程会话无真实控制租约启动模拟采集的后端失败测试，复现 `409 real_control_required`；将启动与停止的模拟会话控制从真实租约门禁分流，并验证真实采集和访客权限边界保持不变

## 12. F 盘隔离客户端验收实验室（2026-09-23）

- [x] 12.1 生成并测试 Windows Sandbox 配置、F 盘白名单暂存脚本、回环 MySQL、虚拟 TCP JSON 源和脱敏证据导出资产；输入目录只读，只有 `F:\AFP_Client_Validation_Lab\results` 可写
- [x] 12.2 在不启用 Sandbox 的阶段完成 F 盘暂存，核对 helper、CSV 和 MySQL 运行文件哈希，确认未复制现有 `data`、`my.ini`、密码、DPAPI 密文或令牌且产品文件哈希不变
- [x] 12.3 经用户在管理员操作前再次确认后，仅启用 `Containers-DisposableClientVM`，由用户决定何时重启，并验证 AFP 公网服务在重启后仍健康
- [ ] 12.4 在 Sandbox 新 Windows 用户中验证真实 helper 首次未配置、配对、同用户重启、五类静态接口保持独立、浏览器上传模拟启停和一个额外回环自定义接口；不得改写五类内置驱动
- [ ] 12.5 用真实 helper 和协议级虚拟 helper 分别执行 50 Hz × 600 秒及 10 秒断线恢复，验证 30,000 行、单在途、ACK 推进、零缺失重复、会话隔离和 P95 不超过 1 秒
- [ ] 12.6 验证 Sandbox 回环 MySQL 的 DPAPI 边界、预检和完整行数，分别记录 helper-local 与 server-target 结果；数据库失败不得删除 CSV，所有导出证据必须通过秘密扫描
- [ ] 12.7 重跑客户端实验室测试及现有 helper、模拟、MySQL、五类接口和公网安全回归，生成 `passed`/`failed`/`not_covered` 计分卡并保持 Sandbox 运行
- [ ] 12.8 在真实第二台电脑及真实 PLC、ABB、热电偶、薄膜压力和 UVC 设备上完成现场验收；在取得独立现场证据前保持未完成，Sandbox 或环回结果不得替代
- [x] 12.9 记录并回归验证干净 Sandbox 的 Visual C++ 运行库前置条件、`root@localhost`/`skip-name-resolve` 首次账号边界，以及 Store Notepad/PATH Edge 不可假定存在；只修复客户端实验引导，不改动产品五类驱动

## 13. 隔离客户端 MySQL 首次保存回归（2026-09-23）

- [x] 13.1 记录用户实测“两条 MySQL 均失败”，分别验证服务器目标配置在宿主 MySQL 停止时返回 2003/10061、Sandbox 本机数据库目录无 AFP 表；不得把两者合并为密码错误
- [x] 13.2 先补失败测试：本机显式首次检查遇到缺失表时初始化并复检、初始化 1142 必须显示真实授权错误、网页请求必须携带首次初始化和回滚写测试标志；真实库复现 1452 后补充父铺层必须早于采样子表的 SQL 顺序测试
- [x] 13.3 仅在 helper-local 的缺失表阶段调用既有 `initialize_schema(create_database=False)`，成功后重新预检，失败时保留实际错误分类；在本机库与目标库共享的 `save_layer` 事务中先写父铺层再流式写采样，保持表结构、外键、CSV、五类接口和预测不变
- [x] 13.4 运行 MySQL/helper/前端/五类接口回归，同步验证过的外置文件并重建 helper，更新交付哈希与客户端实验室固定哈希
- [x] 13.5 在保持 Sandbox 运行的前提下，验证 helper-local 首次建表、预检、实际写入和行数，并在宿主 MySQL 恢复后验证 server-target 预检与实际写入；证据不得包含密码或 DPAPI 密文
- [x] 13.6 更新计分卡和第二台电脑验收说明；只有公网授权页面再次显示两条实际保存成功后，才将相应人工验收项标记通过
- [x] 13.7 为首次建表和中断重试补齐数据库级 `REFERENCES`、`CREATE VIEW`、`SHOW VIEW`、`DROP` 最小权限模板及失败测试，重新同步客户端实验室脚本和服务器授权建议并复验 1142 链路
