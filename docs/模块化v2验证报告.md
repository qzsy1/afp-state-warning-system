# AFP 一体化系统模块化 v2.0.1 验证报告

验证日期：2026-09-01  
源码分支：`feature/modular-runtime-v2`  
发布目录：`F:\AFP_Integrated_Modular_v2\delivery\AFP_Integrated_System_Modular_v2.0.1`

## 验证结果

| 项目 | 结果 | 证据摘要 |
|---|---:|---|
| Python 语法编译 | 通过 | `modular_runtime/app` 与测试代码无语法错误 |
| 模块/补丁单元测试 | 5/5 通过 | 数据契约、正常安装回滚、多文件中途失败原子恢复、越界路径拒绝 |
| MySQL 标识关系单元测试 | 3/3 通过 | 复合试样主键、独立重复隔离、关系视图标识均通过 |
| 源码模式模块装载 | 通过 | 9 个模块全部装载且健康，health 模块可单独重载 |
| 发布软件自检 | 通过 | 版本 2.0.1；入口、界面、数据、模型、两项硬件 DLL 均存在；Tk 8.6、MySQL、openpyxl、xlrd 可实际加载 |
| Tk 文件/文件夹选择 | 通过 | 软件内创建 Tk 根窗口成功；界面实际打开“选择模拟采集 CSV 文件”窗口并完成文件路径回填 |
| 发布软件完整性 | 通过 | SHA-256 检查 5,680 个不可变文件，缺失 0、错误 0、格式错误 0 |
| 本地界面服务 | 通过 | `/api/health` 返回 `status=ok` |
| 模拟采集 | 通过 | 完整链路复测采集 208 个数据点；模拟来源不要求真实传感器接口 |
| 在线三级预警 | 通过 | 窗口级、层级、试样级均生成状态结果 |
| 训练数据整合 | 通过 | 30,816 行、31 列、64 个试样、32 种工况、320 层、321 个有效源文件 |
| 预测与预警训练烟雾测试 | 通过 | CPU 下完成 1 个 epoch，保存预测检查点并完成四种预警模型比较与保存 |
| Excel 训练运行时 | 通过 | 软件内部以 openpyxl 写入并读回含中文列名的 `.xlsx` 文件；2 行、3 列一致 |
| MySQL 界面连接 | 通过 | 界面“检查 MySQL”连接 `127.0.0.1:3306/afp_state_warning` 成功并保持为已连接提示 |
| MySQL 写入与回读 | 通过 | 独立测试库完成自动建表、单层 2 行批量事务写入及关系视图回读，试样复合键正确 |
| 模块补丁与回滚 | 通过 | 从 Git 差异生成 30 文件补丁，软件完成安装、30/30 文件回滚，回滚后完整性仍通过 |

## 模块健康范围

通过健康检查的模块为：`drivers`、`acquisition`、`storage`、`data_integration`、`prediction`、`health`、`warning`、`training`、`diagnostics`。驱动模块确认打包目录含 `BsvUvcNative.dll` 和 `libusb-1.0.dll`；存储模块确认软件内包含并实际使用 MySQL Python 驱动；健康指标模块确认新数据方案为 16 个传感器且健康指标工件存在。v2.0.1 同时补齐 `_tkinter.pyd`、Tcl/Tk 8.6 数据文件、openpyxl 3.1.5 和 xlrd 2.0.2。

## 科学与现场验证边界

- 本次完整链路使用模拟采集数据，未连接真实传感器，因此只能确认真实采集驱动、接口映射和错误处理代码已被装入，不能替代设备现场通信验收。
- MySQL 写入回读使用专门的临时测试库，不把诊断行写入正式实验库；界面仅对正式库执行连接检查。迁移到另一台电脑时仍需确认 MySQL 服务、账户权限和目标数据库配置。
- 一轮训练仅用于证明“导入—整合—训练—保存”链路能够执行，所得验证分数不是论文模型精度，也不能作为真实 AFP 缺陷识别能力结论。
- 当前预警输出属于模型/健康指标证据。没有独立缺陷检测或力学性能证据时，不应把它表述为已确认真实缺陷诊断。

## 可追溯结果

软件运行后诊断结果位于发布目录的 `runtime`：

- `self_test_result.json`
- `verify_files_result.json`
- `integration_smoke_result.json`
- `functional_smoke_result.json`
- `mysql_smoke_result.json`
- `spreadsheet_smoke_result.json`

完整业务烟雾测试报告及其临时训练产物位于 `verification/functional_smoke`。这些目录是可变运行记录，因此不纳入 `SHA256SUMS.txt`；软件主体、外部模块、模型和基础运行时均纳入校验。
