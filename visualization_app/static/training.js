"use strict";
const $=id=>document.getElementById(id);
let imported=false,running=false,lastSeq=0,pollTimer=null,lossRows=[];

async function request(url,options={}){const response=await fetch(url,options);const data=await response.json();if(!response.ok||data.error)throw new Error(data.error||`请求失败 (${response.status})`);return data}
async function post(url,data){return request(url,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)})}
function log(message,data){const line=`[${new Date().toLocaleTimeString()}] ${message}${data?"\n"+JSON.stringify(data,null,2):""}\n`;$('log').textContent+=line;$('log').scrollTop=$('log').scrollHeight}
function number(id){return Number($(id).value)}
function mysqlPayload(){return {host:$('host').value,port:number('port'),user:$('user').value,password:$('password').value,database:$('database').value}}
function columnPayload(){return {condition_columns:$('conditions').value,input_columns:$('inputs').value,output_columns:$('outputs').value}}
function sourcePayload(){return {source:$('source').value,path:$('path').value,mysql:mysqlPayload(),query:$('query').value,data_mode:$('dataMode').value,...columnPayload(),history_length:number('history'),prediction_length:number('prediction'),stride:number('stride')}}

async function loadDefaults(){const mode=$('dataMode').value;const base=mode==='legacy'?'legacy':'new';const d=await request(`/api/training/defaults?mode=${base}`);$('conditions').value=d.condition_columns;$('inputs').value=d.input_columns;$('outputs').value=d.output_columns;$('query').value=d.mysql_query;if(!$('outputRoot').value)$('outputRoot').value=d.output_root;const editable=mode==='other';for(const id of ['conditions','inputs','outputs'])$(id).disabled=!editable;invalidateImport()}
function invalidateImport(){imported=false;if(!running)$('train').disabled=true}
function updateSource(){const kind=$('source').value;const mysql=kind==='mysql';$('mysqlSource').classList.toggle('hidden',!mysql);$('fileSource').classList.toggle('hidden',mysql);$('pathLabel').firstChild.textContent=kind==='complete_csv'?'普通或已整合的单个 CSV ':'CSV 文件夹或单个文件 ';$('pickPath').textContent=kind==='complete_csv'?'选择文件':'浏览';invalidateImport()}

$('dataMode').onchange=loadDefaults;$('source').onchange=updateSource;
for(const id of ['path','conditions','inputs','outputs','history','prediction','stride','query','database'])$(id).addEventListener('input',invalidateImport);
$('initialization').onchange=()=>{const enabled=$('initialization').value==='pretrained';$('pretrained').disabled=!enabled;$('pickModel').disabled=!enabled};
$('pickPath').onclick=async()=>{try{if($('source').value==='complete_csv'){const x=await post('/api/training/select-file',{kind:'csv',initial_path:$('path').value});if(x.selected)$('path').value=x.path}else{const x=await post('/api/acquisition/select-folder',{initial_path:$('path').value});if(x.selected)$('path').value=x.path}invalidateImport()}catch(e){alert(e.message)}};
$('pickModel').onclick=async()=>{try{const x=await post('/api/training/select-file',{kind:'model',initial_path:$('pretrained').value});if(x.selected)$('pretrained').value=x.path}catch(e){alert(e.message)}};
$('pickOutput').onclick=async()=>{try{const x=await post('/api/acquisition/select-folder',{initial_path:$('outputRoot').value});if(x.selected)$('outputRoot').value=x.path}catch(e){alert(e.message)}};

function previewTable(rows){if(!rows.length){$('preview').classList.add('hidden');return}const columns=Object.keys(rows[0]);$('preview').innerHTML=`<table><thead><tr>${columns.map(x=>`<th>${escapeHtml(x)}</th>`).join('')}</tr></thead><tbody>${rows.map(row=>`<tr>${columns.map(c=>`<td>${escapeHtml(row[c])}</td>`).join('')}</tr>`).join('')}</tbody></table>`;$('preview').classList.remove('hidden')}
function escapeHtml(value){return String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function showValidation(v){$('rows').textContent=v.rows??'—';$('conditionsCount').textContent=v.conditions??'—';$('retainedFiles').textContent=v.retained_files??v.accepted_files??'—';$('rejectedFiles').textContent=v.rejected_files??0;const lines=[`结果：${v.ok?'通过':'未通过'}`,`数据行数：${v.rows}；工况种数：${v.conditions}；保留文件数：${v.retained_files??v.accepted_files??0}；剔除文件数：${v.rejected_files??0}`,`独立试样：${v.specimens}；铺层：${v.layers}；预警标签：${v.warning_ready?'正常/异常齐全':'未齐全'}`,`非有限值：${v.nonfinite_values}；过短铺层：${v.short_layers}；要求每层至少 ${v.minimum_points_per_layer} 点`,`标签统计：${JSON.stringify(v.state_counts)}`];if(v.missing_columns?.length)lines.push(`缺失列：${v.missing_columns.join(', ')}`);if(v.warnings?.length)lines.push(`跳过/警告（${v.warnings.length}）：\n${v.warnings.slice(0,8).join('\n')}`);$('precheck').textContent=lines.join('\n');$('precheck').className=`status ${v.ok?'ok':'bad'}`}
$('import').onclick=async()=>{try{$('import').disabled=true;$('precheck').textContent='正在读取、筛选并整合数据……';const x=await post('/api/training/import',sourcePayload());imported=!!x.validation.ok;showValidation(x.validation);previewTable(x.preview||[]);$('train').disabled=!imported;log('数据导入完成',x.validation)}catch(e){imported=false;$('precheck').textContent=e.message;$('precheck').className='status bad';$('train').disabled=true;log('导入失败',{error:e.message})}finally{$('import').disabled=false}};

function settings(){return {training_type:$('trainingType').value,epochs:number('epochs'),patience:number('patience'),batch_size:number('batch'),learning_rate:number('lr'),weight_decay:number('weightDecay'),dropout:number('dropout'),min_delta:number('minDelta'),device:$('device').value,seed:number('seed'),task_name:$('taskName').value,output_root:$('outputRoot').value,pretrained_model:$('initialization').value==='pretrained'?$('pretrained').value:''}}
function setRunning(value){running=value;$('train').textContent=value?'结束并保存':'开始训练';$('train').className=value?'danger':'';$('import').disabled=value;for(const id of ['dataMode','source','trainingType','conditions','inputs','outputs','path','history','prediction','stride','initialization','epochs','patience','batch','lr','weightDecay','dropout','minDelta','device','seed','taskName','outputRoot'])$(id).disabled=value||(['conditions','inputs','outputs'].includes(id)&&$('dataMode').value!=='other');$('pickPath').disabled=value;$('pickOutput').disabled=value;$('pickModel').disabled=value||$('initialization').value!=='pretrained';$('train').disabled=value?false:!imported}
$('train').onclick=async()=>{try{if(running){await post('/api/training/stop',{});$('stage').textContent='正在停止并保存';log('已请求结束训练，正在保存当前模型');return}lossRows=[];drawChart();const x=await post('/api/training/start',settings());setRunning(true);$('stage').textContent='准备数据';$('result').textContent='训练运行中……';log('训练任务已启动',x);poll()}catch(e){alert(e.message);log('训练启动失败',{error:e.message})}};

const stageNames={idle:'等待导入',preparing:'准备与标准化',prediction_training:'预测模型训练',warning_training:'预警模型训练',finished:'训练完成',stopped:'已停止并保存',error:'训练失败'};
function fmt(x){return Number.isFinite(Number(x))?Number(x).toExponential(4):'—'}
function updateProgress(p){$('stage').textContent=stageNames[p.stage]||p.stage||'—';$('epochText').textContent=`${p.epoch||0} / ${p.epochs||0}`;$('patienceText').textContent=`${p.bad_epochs||0} / ${p.patience||0}`;$('bestLoss').textContent=fmt(p.best_validation_loss);const ep=Math.min(100,100*(p.epoch||0)/Math.max(1,p.epochs||1));const pa=Math.min(100,100*(p.bad_epochs||0)/Math.max(1,p.patience||1));$('epochBar').style.width=`${ep}%`;$('patienceBar').style.width=`${pa}%`;$('epochPercent').textContent=`${ep.toFixed(0)}%`;$('patiencePercent').textContent=`${pa.toFixed(0)}%`}
function handleEvent(e){log(e.event,e);if(e.event==='epoch_progress'){lossRows.push(e);drawChart()}if(e.event==='training_finished'||e.event==='training_stopped'){$('result').textContent=`任务目录：${e.task_dir}\n整合CSV：${e.complete_csv}\n预测模型：${e.checkpoint}${e.warning?'\n预警模型：'+e.warning.selected_model:''}`}if(e.event==='training_error')$('result').textContent=`训练失败：${e.error}`}
async function poll(){clearTimeout(pollTimer);try{const x=await request(`/api/training/status?after_seq=${lastSeq}`);for(const e of x.events||[])handleEvent(e);lastSeq=x.last_seq||lastSeq;updateProgress(x.progress||{});if(x.result&&!running)$('result').textContent=`任务目录：${x.result.task_dir}\n整合CSV：${x.result.complete_csv}\n预测模型：${x.result.checkpoint}`;if(running&&!x.running)setRunning(false);else if(!running&&x.running)setRunning(true);if(x.error)$('result').textContent=`训练失败：${x.error}`;pollTimer=setTimeout(poll,x.running?650:1800)}catch(e){log('状态连接失败',{error:e.message});pollTimer=setTimeout(poll,2500)}}

function drawChart(){const c=$('lossChart'),ctx=c.getContext('2d'),w=c.width,h=c.height;ctx.clearRect(0,0,w,h);ctx.fillStyle='#fff';ctx.fillRect(0,0,w,h);ctx.strokeStyle='#dce5ec';ctx.lineWidth=1;for(let i=0;i<=5;i++){const y=22+(h-55)*i/5;ctx.beginPath();ctx.moveTo(50,y);ctx.lineTo(w-18,y);ctx.stroke()}ctx.fillStyle='#667d8f';ctx.font='12px Segoe UI';ctx.fillText('Loss',10,20);if(!lossRows.length){ctx.fillText('训练开始后显示实时损失曲线',w/2-85,h/2);return}const vals=lossRows.flatMap(r=>[Number(r.train_loss),Number(r.validation_loss)]).filter(Number.isFinite),min=Math.min(...vals),max=Math.max(...vals),range=Math.max(1e-9,max-min);const plot=(key,color)=>{ctx.strokeStyle=color;ctx.lineWidth=2;ctx.beginPath();lossRows.forEach((r,i)=>{const x=50+(w-70)*(lossRows.length===1?.5:i/(lossRows.length-1));const y=22+(h-55)*(max-Number(r[key]))/range;i?ctx.lineTo(x,y):ctx.moveTo(x,y)});ctx.stroke()};plot('train_loss','#1677b8');plot('validation_loss','#d75a42');ctx.fillStyle='#1677b8';ctx.fillText('训练损失',60,h-10);ctx.fillStyle='#d75a42';ctx.fillText('验证损失',135,h-10)}

window.addEventListener('resize',drawChart);loadDefaults().then(()=>{updateSource();drawChart();poll()}).catch(e=>log('初始化失败',{error:e.message}));
